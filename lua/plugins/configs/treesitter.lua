local options = {
  ensure_installed = { "lua", "python", "c", "vim", "vimdoc"},

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
